__pycache__/
*.pyc
# --- secret guard (Public repo・auto-git-sync誤commit防止) ---
.env
*.env
token.json
*token*.json
*secret*
credentials*.json
client_secret*.json
*.key
# --- 退避ファイルと同期ログは公開リポジトリに入れない（2026-07-29）---
*.bak_*
auto-git-sync.log
